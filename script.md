1. Создание приложений

```shell
mkdir django;
mkdir tornado;
mkdir aiohttp;
mkdir fastAPI;
mkdir flask;
mkdir hug;
mkdir database;
```

2. Создадим виртуальное окружение
```shell
python3 -m venv venv
source venv/bin/activate.fish
```